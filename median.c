#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct get_input(int input){
		scanf("d",&inputvar);
		int a = [];
		strcpy(a[0], inputvar);
		scanf("d",&inputvar2);
		strcpy(a[1], inputvar2);
};

struct inout(){
		int nums1 = [1,3];
		int nums2 = [2]
		int c;
		int csize = 3;
		for ( i = 0; i< csize; i++)
			if (nums1[i] < nums2[i])
			 c[i] = nums1[i];
			 c[i+1] = nums2[i];

			 return c[i];
}

struct compare() {
		if (csize / 2 == 0)
			return ((csize / 2) + ((csize / 2 ) + 1));

}
int main(){
		inout();
		compare();  
}